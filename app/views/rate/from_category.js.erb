$(document).ready(function(){
    var imageBox = $('#carousel');
    imageBox.show();
    imageBox.empty();
    imageBox.html("<%= j render partial: 'rate/images', locals: { images: @selected } %>");
    imageBox.slick({
        dots: true,
        variableWidth: true,
        draggable: false
    });
});